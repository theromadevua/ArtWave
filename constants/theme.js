import { hp, wp } from "../helpers/common"

export const colors = {
    BLACK: '#000000',
    WHITE: '#ffffff',
    GRAY: '#c9c9c9',
    DARK_GRAY: "#333333",
    LIGHT_GRAY: 'rgba(0,0,0,0.06)',
    ROSE: '#ffc4c4'
}

export const paddings = {
    lp: wp(1) > hp(1) ? wp(10) : 15
}